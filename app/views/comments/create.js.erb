var comment = {
  'id': "<%= @comment.id %>",
  'body': "<%= @comment.body %>",
  'when': "<%= @comment.created_at.strftime('%e %b %Y at %H: %M') %>",
  'whenInWords': "<%= time_ago_in_words(@comment.created_at) %>",
  'userName': "<%= @comment.user.name %>",
  'userAvatar': "<%= default_avatar(@comment.user) %>",
  'userLink': "<%= profile_path %>",
  'deleteLink': "<%= post_comment_path(@post, @comment) %>"
};

console.log(comment);

Comments.displayComment(comment);